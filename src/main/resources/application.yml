server:
  port: 8088

logging:
  level:
    org.springframework: DEBUG

spring:
  messages:
    basename: messages
  security:
    user:
      name: username
      password: passw0rd
  jpa:
    show-sql: true


management:
  endpoints:
    web:
      exposure:
        include: "*"
